import { YearBetween } from './year-between';

describe('YearBetween', () => {
  it('should create an instance', () => {
    const directive = new YearBetween();
    expect(directive).toBeTruthy();
  });
});
