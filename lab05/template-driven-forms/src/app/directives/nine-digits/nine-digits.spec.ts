import { NineDigits } from './nine-digits';

describe('NineDigits', () => {
  it('should create an instance', () => {
    const directive = new NineDigits();
    expect(directive).toBeTruthy();
  });
});
