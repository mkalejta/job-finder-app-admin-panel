import { FirstLetterBig } from './first-letter-big';

describe('FirstLetter', () => {
  it('should create an instance', () => {
    const directive = new FirstLetterBig();
    expect(directive).toBeTruthy();
  });
});
